# Validated in CI against .env.example (see env-validation workflow).
# DSL: required(<prefix?>) | optional(<default>)
# EXAMPLE_API_KEY=required(sk-)
# PUBLIC_BASE_URL=optional(http://localhost:3000)
