# Documents the keys the admin app needs. Committed on purpose; the managed
# .gitignore region re-includes it after ignoring .env and .env.*.
NEXT_PUBLIC_API_URL=https://api.example.invalid
ADMIN_SESSION_SECRET=replace-me
