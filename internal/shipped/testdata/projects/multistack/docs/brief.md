# Multistack — Product Brief

*Draft v0.1*

## One-liner

One product, three surfaces.

## Stack

`ios-web-supabase`. The split is by directory rather than by repository so a
change that spans the app and its backend is one pull request and one review.
