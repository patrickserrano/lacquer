# Rootapp — Product Brief

*Draft v0.1*

## One-liner

A list you can actually finish.

## Stack

`ios` — a native iOS app, nothing else. Root layout: one stack, so the Xcode
project, its lint configs and its CI all read more simply at the repository
root.
