# Spmkit — Product Brief

*Draft v0.1*

## One-liner

The parsing library the apps share.

## Stack

A bare SwiftPM package. The lacquer ships no `swift` profile yet, so nothing
gates this repository beyond the core rules — which `lacquer audit` reports on
every run.
