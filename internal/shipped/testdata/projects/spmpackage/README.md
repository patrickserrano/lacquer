# Spmkit

A library distributed as a SwiftPM package. There is no `.xcodeproj` anywhere in
the repository, which is what makes this a Swift *stack* rather than a local
module of some app — see `Packages/RootappKit` in the `rootapp` fixture for the
other case.
